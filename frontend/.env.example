# Leave empty in development — Vite proxy handles /api and /auth routes
# In production, set to your Railway backend URL (no trailing slash)
# Example: VITE_API_URL=https://azitrip-backend.up.railway.app
VITE_API_URL=
